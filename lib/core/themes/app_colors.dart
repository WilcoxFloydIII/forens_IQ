import 'package:flutter/material.dart';

class AppColors {
  // Brand color (used for headings and specific buttons)
  static const Color brand = Color(0xFF12A4ED);
  static const Color brandSecondary = Color(0xFF293338);
  static const Color brandBackground = Color(0xFF121717);
  static const Color bottomNavBar = Color(0xFF1C2426);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9EB0BA);

  // Status colors
  static const Color error = Color(0xFFEB001B);

  // Other colors
  static const Color linearBar = Color(0xFF3B4A54);
  static const Color divider = Color.fromRGBO(229, 232, 235, 1);
}
